apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - name: my-container
      image: myregistry.com/my-image:latest
      ports:
        - containerPort: 80
  imagePullSecrets:
    - name: my-registry-secret
